# API 엔드포인트 (프로덕션 빌드 시 최우선 적용)
VITE_API_BASE_URL=YOUR_REST_API_ENDPOINT
VITE_WS_URL=YOUR_WEBSOCKET_ENDPOINT

# 서비스 설정
VITE_APP_TITLE=YOUR_SERVICE_NAME
VITE_APP_DESCRIPTION="AI 콘텐츠 생성 서비스"

# 기타 설정
VITE_ENABLE_NEWS_SEARCH=true
VITE_ENV=production

# 중요: Vite는 프로덕션 빌드(npm run build) 시
# .env.production 파일을 .env 파일보다 우선적으로 읽습니다.
# 따라서 이 파일의 설정이 최종적으로 적용됩니다.