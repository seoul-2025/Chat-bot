service: nexus-title-external-three

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-northeast-2
  stage: ${opt:stage, 'dev'}

functions:
  main:
    handler: handler.handler
    events:
      - http:
          path: /title/external/three
          method: POST
          cors: true
